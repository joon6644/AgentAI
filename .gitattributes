# 줄바꿈 정규화: 저장소에는 항상 LF 로 저장
* text=auto

# 셸 스크립트는 반드시 LF (CRLF 이면 Git Bash / Linux 에서 실행 오류)
*.sh text eol=lf

# PowerShell 스크립트는 Windows 기본값인 CRLF
*.ps1 text eol=crlf

# 잠금 파일은 생성물로 표시 (diff 노이즈 감소)
uv.lock linguist-generated=true
